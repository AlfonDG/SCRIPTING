Shell es interpretado, no compilado: El interpretado es que directamente lanza la tarea sin necesidad de compilar.

Los compilados como Pyrthon pasan de lenguaje máquina a lenguaje máquina.
----------------------------------------------------------------------------------
1. Bash Script es interpretado, NO compilado

2. sh SÍ que puede ejecutarse en BASH pero viceversa no. Solamente SH es compatible con BASH.

3. Los scripts se ejcutan con un PID diferente. Esto se le llama hacer FORKING, generan un subproceso

4. wich - a "nombre_script" o funciones que tenga Shell Script para comprobar que 
